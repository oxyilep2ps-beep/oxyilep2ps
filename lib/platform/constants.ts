/** Platform-wide fixed annual interest rate (Phase 19). */
export const FIXED_INTEREST_RATE = 10;

export const FIXED_INTEREST_RATE_LABEL = 'Fixed Interest Rate: 10%';

/** Allowed loan tenure options (months) for marketplace apply flow. */
export const TENURE_OPTIONS = [6, 12, 24, 36] as const;
