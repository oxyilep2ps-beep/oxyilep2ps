import { AdminEmailBroadcastTab } from '@/components/admin/admin-email-broadcast-tab';

export const metadata = {
  title: 'Email Broadcast — Oxyile Admin',
};

export default function AdminBroadcastPage() {
  return <AdminEmailBroadcastTab />;
}
