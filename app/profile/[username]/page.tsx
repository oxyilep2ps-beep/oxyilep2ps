import { PublicUsernameProfileView } from '@/components/profile/public-username-profile-view';

export const metadata = { title: 'Member Profile | Oxyile' };

export default function PublicProfileByUsernamePage() {
  return <PublicUsernameProfileView />;
}
